model Project {
  id          String   @id @default(cuid())
  title       String
  description String
  stack       String[] 
  githubUrl   String?
  liveUrl     String?
  imageUrl    String?
  
  createdAt   DateTime @default(now())
}