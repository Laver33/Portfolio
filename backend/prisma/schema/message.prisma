
model Message {
  id         String  @id @default(cuid())
  name       String
  email      String @unique
  theme      String
  message    String

  isRead     Boolean  @default(false)
  createdAt  DateTime @default(now())
}